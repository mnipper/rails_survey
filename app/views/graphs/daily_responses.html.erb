<section class="widget" ng-controller="DailyGraphCtrl" ng-init="initialize(<%= current_project.id %>)">
  <header>
    <h4><i class="icon-calendar"></i> Response count per day for project <b> <%= current_project.name %> </b> </h4>
  </header>
  <div class="body">
    <sc-Daily-Responses val="data"> </sc-Daily-Responses>
    <h4> Response count per hour for project <%= current_project.name %> </h4>
    <!--  <nvd3-line-chart
        data="lineData"
        showXAxis="true"
        showYAxis="true"
        tooltips="true"
        interactive="true">
      </nvd3-line-chart> -->
  </div>
</section>
