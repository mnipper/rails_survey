<section class="widget">
<%= form_for([current_project, @instrument, @section]) do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>
      <ul>
        <% @section.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
    <div class="field top-buffer">
        <%= f.label :title, 'Section Title' %> <br />
        <%= f.text_field :title, class: 'form-control' %>
    </div>
    
    <div class="field top-buffer">
        <%= f.label :start_question_identifier, 'Beginning of section question identifier' %> <br />
        <%= f.text_field :start_question_identifier, class: 'form-control' %>
    </div>
    
    <div class="top-buffer">
      <%= f.submit class: 'btn btn-primary' %>
    </div>
<% end %>
</section>